applications:
  - name: monitor-plans-api
    path: .
    command: npm run start
    memory: 512M
    env:
      OPTIMIZE_MEMORY: true
    # services:
    #   - camd-pg-db
    # instances: 2
    # health-check-type: http
    # health-check-http-endpoint: /swagger
    routes:
      - route: easey-dev.app.cloud.gov/api/monitor-plans
      - route: easey-dev.app.cloud.gov/api/monitor-locations
