applications:
  - name: monitor-plan-api
    path: .
    command: npm run start
    memory: 512M
    env:
      EASEY_API_HOST: ((host))
      OPTIMIZE_MEMORY: true
    routes:
      - route: ((host))/api/monitor-plan-mgmt
    services:
      - camd-pg-db
